angular
    .module('cmps.timeline')
    .controller('TimelineCtrl', TimelineController);

/**
 * @ngdoc controller
 * @name cmps.timeline:TimelineCtrl
 *
 * @requires $scope
 *
 * @description
 * The timeline component
 */
TimelineController.$inject = [
    '$scope',
];

function TimelineController($scope) {
    var self = this;
}
